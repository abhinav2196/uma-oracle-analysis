## Subgraphs Registry

Artifacts generated by verification live here:

- `REGISTRY.json`: machine-readable verification results
- `REGISTRY.md`: human-readable summary table

Generate/update with:

```bash
make verify-subgraphs
```

Set your Graph API key before running:

```bash
export THE_GRAPH_API_KEY=YOUR_KEY
```


